<h1>タスク一覧</h1>
<table class="task_table">
  <tr>
    <th>content</th>
  </tr>
  <% @tasks.each do |task| %>
    <tr>
      <td><%= link_to task_path(task.id), remote: true do %>
        <%= task.content %>
      <% end %></td>
    </tr>
  <% end %>
</table>
